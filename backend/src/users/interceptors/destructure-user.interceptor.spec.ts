import { DestructureUserInterceptor } from './destructure-user.interceptor';

describe('DestructureUserInterceptor', () => {
  it('should be defined', () => {
    expect(new DestructureUserInterceptor()).toBeDefined();
  });
});
